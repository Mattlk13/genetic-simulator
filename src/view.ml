open Population
open Individual
open Map

(* Dumb view.ml file to compile the project without SDL *)

(* ========== Drawing functions ========== *)

let draw_rect ~x ~y ~height ~width ~color ~surface = ()

let draw_map map = ()

let draw population individuals = ()

(* ========== View general functions ========== *)

let init () = ()

let create size = ()

let reset () = ()

let update () = ()

let check_event () = ()